# 论云原生架构及其应用

> 项目素材：sunwu-claw（数字员工运营管理平台）
> 时间线：2024-11 启动 → 2026-06 交付试运行，历时 20 个月，投入约 400 万元
> 主线：从单体集中式 → 云原生架构的演化过程与决策（CBSD 构件化集成，非自研拆分；服务网格见第二篇）
> 状态：数据已按真实口径填写（200 Agent / 50 QPS / 2天发布）

---

## 摘要

2024年11月，我参与了某公司"数字员工运营管理平台"项目的架构设计与开发，担任系统架构设计师，负责整体架构设计与技术选型。该系统以多智能体（Agent）的注册、部署、记忆同步、能力装配与协作治理为核心，采用了构件化软件开发（CBSD）方法，直接集成 OpenClaw 运行时、ClawHub 技能市场、飞书渠道桥接等开源构件，平台自身聚焦于 Agent 实例管理、Skill 管理与记忆管理等编排能力。项目总投入约400万元，历时20个月，于2026年6月与合作单位共同交付试运行，获得客户一致好评。项目启动初期采用单体集中式架构，随着集成构件持续增多，部署单元增长至 7 个，环境变量增至 78 项，集成复杂度急剧上升。本文结合笔者的实际工作经验，论述了该项目基于 CBSD 集成、从单体架构向云原生架构演化的过程，包括：容器化改造与构件标准化交付、Kubernetes 编排与 Agent 状态外置、可观测性与 GitOps 自动化交付三个方面。

---

## 背景

随着人工智能技术的快速普及，越来越多的企业开始部署大量 AI 智能体（Agent）处理日常业务，但随之而来的却是沉重的运维负担：中心记忆仓库更新需要人工 SSH 登录每台机器逐个执行；新建 Agent 需要手工注册账号、配置邮箱、编排工作流，耗时数小时；Agent 状态、Token 消耗、任务进度均不可视。为从根本上解决上述问题，该公司决定投资建设一套统一的数字员工运营管理平台。我所在的公司承担该项目，我被任命为系统架构设计师，负责整体架构设计与中间件选型。项目组共20人，项目于2024年11月正式启动，历时20个月，于2026年6月与合作单位共同交付试运行。系统核心功能包括 Agent 生命周期管理、MCP 工具治理、Skill 能力装配、记忆同步与协作治理。

---

## 一、项目的总体结构与个人职责

本平台以 Agent 生命周期管理为主线，覆盖"注册→部署→能力装配→记忆同步→协作治理"全生命周期。架构上采用构件化软件开发（CBSD）方法，直接集成 OpenClaw（Agent 运行时）、ClawHub（Skill 市场）、飞书渠道桥接等开源构件，并在此基础上构建管理编排层，主要功能模块包括：Agent 实例管理——维护每台实例的部署位置与运行状态（在线/离线/工作中）；Skill 能力装配——实现技能的搜索、安装、分发与版本管理；记忆管理——基于 Git 仓库实现组织/项目/角色/Agent 四层记忆分发；LLM 统一接入网关——承担多模型路由与计费；以及会话数据分析引擎。平台以 Next.js 16 为核心，LLM 网关基于 NestJS 11，数据分析引擎基于 Python + Prefect，Gateway 服务以 Go 实现 MCP 工具接入。

我作为系统架构设计师，主持了 CBSD 构件选型与集成、容器化、Kubernetes 资源设计、网络策略与 CI/CD 流水线的架构设计，并主导了 Prefect、Nexus 等中间件的选型落地。

## 二、架构演化的动因分析

在项目早期，系统以单体集中式架构运行：所有平台逻辑集中在一个 Next.js 应用中，Agent 通过 SSH 远程管理，配置依赖机器上的环境变量与启动参数。随着业务快速演进，以下问题日益突出：

第一，**CBSD 集成规模扩大，集成难度上升**。平台集成的构件不断增多，构件间通过环境变量、端口、协议相互依赖，任一构件的升级或配置变更都面临连锁影响，集成回归成本高。

第二，**配置与依赖爆炸**。环境变量增长至 78 项，各构件对运行环境的要求各不相同，主机级部署难以统一，构建与部署依赖人工，极易因环境差异导致"在我机器上能跑"的问题。

第三，**治理逻辑与业务逻辑耦合**。MCP 工具的风险路由、提示注入防护等横切关注点散落在平台代码中，难以独立演进与复用。

基于上述动因，我们决定向云原生架构演化，将"基础设施的关注点"（部署、编排、网络、配置、可观测性）从平台代码中剥离，交由平台层统一管理，使 CBSD 集成出的能力能够以标准化的方式交付与运维。

由此确立本文的总论点：**云原生架构的本质，是通过把基础设施关注点从业务代码中最大化剥离，使数字员工平台的运行状态外置化、交付声明化、运维自动化、运行可观测化**。下面围绕这一总论点，从容器化与构件标准化交付、Kubernetes 编排与 Agent 状态外置、可观测性与自动化交付三个分论点展开论述。

## 三、云原生架构的演化实践

在方案设计阶段，我们以云原生架构的原则与模式为指引，确保改造方向符合其核心理念。云原生架构的本质，是把处理弹性、韧性、安全、可观测性等非功能特性的"非业务代码"最大化剥离，交由云设施接管，使业务代码与三方构件只关注自身逻辑。其设计原则包括：服务化、弹性、可观测、韧性、所有过程自动化、零信任、架构持续演进；主要架构模式包括：服务化、Mesh 化、Serverless、存储计算分离、分布式事务、可观测、事件驱动。同时，微服务设计需遵循四类约束——个体约束（单一职责、低耦合）、横向关系（可发现性、可交互性）、纵向约束（数据存储隔离，数据是服务的私有资产）、全局分布式约束（故障发现时效性与根因精确性）。本项目正是围绕这些原则与模式逐项落地的。

### 分论点一：容器化改造与构件标准化交付（解决"环境依赖、集成难维护"）

构件化集成面临的核心问题是异构运行环境与版本漂移，容器化通过镜像封装每个构件的完整运行环境，实现"一次构建、到处运行"，是云原生演化的第一步。

我们将平台自身与所集成的构件分别容器化：平台主体采用多阶段 Dockerfile 构建，以 node:22-alpine 为基础镜像，将依赖安装、构建、运行分离，最终产物仅含运行时与 `.next/standalone` 输出；数据分析引擎基于 Prefect 官方镜像构建；Gateway 服务采用 Go 编译为单一二进制。每个构件独立成镜像，彻底消除环境差异。

在是否容器化的决策上，我们权衡了资源开销与消除环境漂移的价值，鉴于构件异构性高、迭代频繁，最终选择全量容器化。实践表明，新环境从"配置数小时"缩短为"拉镜像即运行"，构件升级由整体重建改为替换镜像 Tag，集成回归范围从"全量回归"收敛为"仅验证所涉构件"。

### 分论点二：Kubernetes 编排、Agent 状态外置与零信任安全（解决"资源调度、状态管理与安全边界"）

Kubernetes 提供统一的调度、编排与自愈能力，同时以 StatefulSet 与状态机管理有状态实例；网络策略实现服务间的最小化访问。对于数字员工平台，Agent 实例天然是有状态的（在线状态、记忆、会话、沙箱），其状态管理正是云原生能力的关键应用场景。

在编排与状态层面，我们引入 Kubernetes 集群，为核心服务定义 Deployment，配置就绪与存活双探针（/health）实现滚动更新与故障自愈，对高并发的 LLM 网关配置 HPA（以 CPU 70% 为阈值，1 至 5 副本自动伸缩），体现**韧性**与**弹性**原则。针对 Agent 实例的有状态特性，采用 StatefulSet 管理实例生命周期，通过状态机与心跳机制统一登记并监控实例状态；Agent 状态、记忆、会话等资产持久化至 PostgreSQL 与阿里云 OSS，实现"状态外置"，实例重建后状态可恢复，体现**存储计算分离**模式，也落实微服务**纵向约束**中的"数据存储隔离"。

在安全层面，针对 LLM 网关设计 NetworkPolicy 实施零信任：入口仅允许核心平台与 Agent 集群访问其 3001 端口，出口仅放行 DNS、PostgreSQL（5432）与外部 LLM 提供商的 HTTPS（443），体现**零信任**原则；并通过 WebSocket 反向代理提供对远端实例与沙箱的安全运维通道。

相比 Docker Compose 的单机编排，Kubernetes 提供细粒度资源配额、自动伸缩、有状态实例管理与网络策略能力，代价是运维复杂度上升，我们通过基础设施即代码（Terraform）将复杂度收敛于平台层。实践表明，高峰期自动扩容、低谷自动回收，Agent 实例状态可迁移、可恢复；网络策略将暴露面收敛为 3001/5432/443 三个端口，攻击面可穷举、可审计。

### 分论点三：可观测性、工作流编排与 GitOps 自动化交付（解决"可观测与部署自动化"）

云原生架构强调可观测、所有过程自动化与持续交付：只有让系统运行状态可度量、任务执行可编排、部署变更可声明式管控，才能实现"无人值守"的稳定运营。

在可观测性层面，我们构建了 Logging、Metrics、Tracing 三支柱，实现多维度可观测，监控告警通过飞书渠道桥接（Webhook）自动触达相关人员，运维无需值守即可获知异常。在任务治理层面，会话数据分析涉及大量长时任务，裸 CronJob 难以管理依赖与失败重试；我引入 Prefect 作为工作流引擎，通过 Kubernetes work pool 将每个 Flow 提交为独立 K8s Job，支持 DAG 依赖、失败重试与幂等控制，将每日数据分析、胜任力快照等任务编排为夜间流水线，体现**事件驱动**与**所有过程自动化**原则。在交付层面，采用 GitOps 模式：GitHub Actions 按代码变更自动重建镜像并推送至私有 Nexus 仓库，再由流水线更新部署仓库中的镜像 Tag，实现环境隔离（staging/prod）与变更可审计，做到面向终态的声明式交付。

相比自建调度器，Prefect 提供成熟的 DAG 与重试机制，降低开发成本；相比直接更新集群，GitOps 保证部署变更可回滚、可审计。实践表明，三支柱可观测使会话与任务链路可追踪、故障可定位；数据分析任务按计划稳定执行；部署全自动、可追溯，平均每 2 天发布一个新版本，异常实例秒级自动摘除重启。此外，架构遵循"持续演进"原则——当前以 Kubernetes 承载服务，后续将随规模增长引入 Serverless 处理突发轻量任务，并评估服务网格（Mesh 化）进一步下沉连接、安全与流量治理能力。

---

## 结尾

经过近20个月的项目开发，数字员工运营管理平台于2026年6月与合作单位共同交付试运行。目前平台管理 Agent 数量超过200个，日均处理任务1000余项，LLM 网关峰值并发达到50 QPS，系统可用性保持在 99.9% 以上，客户反馈良好。在发布交付层面，得益于 GitOps 流水线，平台平均每2天发布一个新版本，过程全自动、可回滚，早期人工逐机部署被彻底自动化；配合双探针，异常实例秒级自动摘除重启，故障恢复不再依赖人工。云原生架构的落地，使系统具备了快速交付、弹性伸缩与高可用的能力，为公司节省了大量运维人力。回顾整个项目，我最大的体会是：数字员工平台治理的难点集中于 **Agent 的状态管理**——Agent 天然是有状态、长生命周期的实体，其在线状态、记忆、会话与沙箱资产分散在多台实例上，只有通过状态外置化、统一登记与可恢复设计，才能真正做到可管、可控、可迁移；这也是本项目云原生改造价值最集中的落点。当然，本项目中仍存在一些不足，例如多集群容灾尚未完全实现，跨集群备份与容灾切换仍在完善中，后续我们将结合服务规模增长逐步完善。我将不断学习，提升自身架构设计能力，为企业数字化转型贡献自己的力量。

---

## 附：真实技术栈核对表（勿编造）

| 层 | 真实技术（已从仓库确认） |
|----|------------------------|
| 前端/主应用 | Next.js 16.2.4 + React 19 + TypeScript |
| LLM 网关 | NestJS 11（@nestjs/core）+ TypeORM + Drizzle ORM |
| 数据分析引擎 | Python + Prefect 3.7.7（prefecthq/prefect:3.7.7-python3.11 镜像） |
| Gateway 服务 | Go（sunwu-gateway，MCP server 实现） |
| 数据库 | PostgreSQL（pg 驱动 8.x）+ Drizzle ORM |
| 文件存储 | 阿里云 OSS（ali-oss，src/lib/s3.ts） |
| Agent 状态管理 | 实例状态机 + StatefulSet + 心跳（terraform-agent.ts 更新 deploy_instances.status） |
| 沙箱 | ws-sandbox（WebSocket 沙箱终端，virtual-agents/ws-sandbox） |
| 云浏览器/云手机 | cloud-phone（VNC/noVNC 控制、资源池、租约与结算） |
| 认证 | next-auth v5 + jose（JWT）+ bcryptjs |
| K8s 交互 | @kubernetes/client-node（WebShell/VNC 双向代理） |
| 工作流 | Prefect 3.7.7（K8s work pool → Job） |
| 交付 | GitHub Actions + 独立部署仓库（GitOps）+ 私有 Nexus 镜像仓库 |
| 基础设施 | Terraform（terraform_template/）+ 阿里云 EDS |
| 网络 | K8s NetworkPolicy + Service + HPA + 双探针 |
| CBSD 集成构件 | OpenClaw（Agent 运行时）、ClawHub（Skill 市场）、飞书 Lark CLI、Prefect、Hermes |
| 未使用 | ❌ Redis / MinIO / Kafka / RabbitMQ / Istio / Langfuse（勿写入论文） |

## 附：论文数据口径（已按真实数据填写）

| 项 | 值 | 说明 |
|----|-----|------|
| 项目启动 | 2024年11月 | 按快2年口径 |
| 项目上线 | 2026年6月 | 与合作单位共同交付试运行 |
| 历时 | 20 个月 | |
| 投入 | 约400万元 | 20万/月 × 20月 |
| 团队人数 | 20 人 | |
| Agent 数量 | 200 个 | 生产环境 |
| 日均任务 | 1000 余项 | |
| LLM 网关并发 | 50 QPS | 50 人同时使用 |
| 发布频率 | 平均2天/版本 | 与 git 提交密度吻合 |
| 可用性 | 99.9% | |
| MTTR | 探针秒级自动恢复 | 相对表述，不编具体数字 |